/**
 * Don't have to import it, we only use unocss tip for dev in this pkg.
 * (we don't use unocss generated css in this pkg, we only mark class.)
 */
// import 'uno.css'
import Button from './button.vue'

// todo: need with install function and prefix, refer element-plus
export {Button}
