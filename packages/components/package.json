{
  "name": "@nev-ui/vue",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "wzc520pyfm",
  "main": "src/index.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "stub": "pnpm build:fast --watch",
    "build": "tsup src --dts",
    "build:fast": "tsup src",
    "prepack": "clean-package",
    "postpack": "clean-package restore",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "vue": ">=3.4.0"
  },
  "dependencies": {
    "@nev-ui/button": "workspace:^",
    "@nev-ui/icon": "workspace:^",
    "@nev-ui/spinner": "workspace:^"
  },
  "devDependencies": {
    "clean-package": "^2.2.0",
    "vue": "^3.4.27"
  },
  "clean-package": "../../clean-package.config.json"
}
