{
  "name": "@nev-ui/interactions",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "wzc520pyfm",
  "main": "src/index.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "stub": "pnpm build:fast --watch",
    "build": "tsup src --dts",
    "build:fast": "tsup src",
    "prepack": "clean-package",
    "postpack": "clean-package restore",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "vue": ">=3.4.0"
  },
  "dependencies": {
    "@nev-ui/use-focus": "workspace:^",
    "@nev-ui/use-hover": "workspace:^",
    "@nev-ui/use-press": "workspace:^"
  },
  "devDependencies": {
    "clean-package": "^2.2.0",
    "vue": "catalog:"
  },
  "clean-package": "../../clean-package.config.json"
}
